---
name: k-humanizer
description: "한국어 초안의 AI식 상투구, 번역투, 추상 명사화, 기계적 병렬·대구, 과잉 완곡, 균일한 문장·문단 리듬, 용어 이관 후 동어반복·연어 충돌, 대화형 AI 서식을 진단하고 뜻과 저자 목소리를 보존하며 자연스러운 한국어로 다시 쓴다. 'AI 티 없애줘', 'ChatGPT 문투를 벗겨 줘', '사람이 쓴 것처럼', 한국어 문체 진단·윤문, 번역투·용어집 검토, 저자 목소리 복원, DOCX 서식 정리에 사용한다. 저자 판별이나 AI 탐지 회피에는 사용하지 않는다."
---

# K-humanizer

기계적으로 느껴지는 지배 패턴을 실제로 해체하되 뜻·귀속·수치·용어와 저자의 살아 있는 문장을 지켜라. 진단의 보수성과 윤문의 실행력을 구분하라. `AI 티 제거`, `사람이 쓴 것처럼`은 기본적으로 **표준 인간화** 요청이다.

## 1. 모드를 먼저 정하라

| 모드 | 선택 조건 | 결과 |
| --- | --- | --- |
| `진단` | 문제 위치와 이유만 요청 | 원문을 고치지 않고 지배 패턴 3~6개 또는 `수정 없음`을 보고 |
| `최소 윤문` | `최소`, `보수적으로`, 특정 패턴만 명시 | 결정적인 표지만 국소 수정 |
| `표준 인간화` | AI 티·ChatGPT 문투 제거, 자연스럽게, 사람이 쓴 것처럼 | 지배 패턴을 문장·문단 단위로 해체하고 윤문본을 완성 |
| `목소리 복원` | 저자 표본·이전 원고를 함께 제공하거나 목소리 보존을 강조 | 같은 저자의 강한 구간에서 로컬 기준선을 만들고 그 범위 안에서 윤문 |

모드별 세부 계약은 [references/runtime-contract.md](references/runtime-contract.md)를 읽어라. 입력의 내력은 `raw_ai`, `ai_edited`, `human_draft`, `human_polished`, `unknown` 중 하나로 기록하라. `human_polished`에서는 문제가 없다는 결론을 허용하되, 사용자가 표준 인간화를 명시했으면 확인된 지배 패턴을 그대로 두지 마라.

## 2. 입력과 범위를 고정하라

1. 장르·독자·목표 격식·번역 원문 유무·입력 내력·파일 형식을 확인하라. 기본 장르는 `essay`, 기본 내력은 `unknown`이다.
2. 단행본·연재물·다파일 원고는 편집 브리프와 manifest를 만들고 목차·서지·각주·부록을 산문에서 분리하라.
3. 전문 검토를 명시하면 [references/full-corpus-review.md](references/full-corpus-review.md)의 구간 대장을 사용하라. 끝낼 수 없으면 부분 검토·전수 앵커 검토 같은 합리적인 대안을 제시하라.
4. 파일 형식별 처리는 [references/format-profiles.md](references/format-profiles.md)를 따르라. Markdown의 제목·굵은 표지·이모지·코드 펜스는 사용자가 서식 재설계를 요청하지 않는 한 삭제·이동·이름 변경하지 마라. DOCX는 의미 있는 스타일과 장식성 서식을 구분하라.
5. 이미 K-humanizer로 고친 글을 다시 검수하면 최초 입력을 `baseline`, 직전 채택본을 `previous`로 고정하고 회차를 기록하라. 직전본만 보고 다시 쓰지 마라.
6. 번역 용어나 표준어를 책 전체에서 바꾸면 [references/terminology-migration.md](references/terminology-migration.md)를 읽고 원어·기존어·확정어·범위·예외를 term map으로 고정하라. 확정어 자체는 보호하되 주변 문장은 다시 읽어라.

## 3. 보호하고 진단하라

1. 수치·날짜·직접 인용·조문 인용 표기·사용자 지정 문자열을 잠가라. 이름·전문용어·따옴표 없는 법령·판결문 본문은 `--protect-file`이나 보호 블록으로 명시하라.
2. [references/ai-style-taxonomy.md](references/ai-style-taxonomy.md)에서 어휘·문장·문단·리듬·서식 층을 모두 확인하라. 빠른 위치 후보는 `scripts/scan_style.py`로 찾되 스캔 횟수를 품질 점수로 쓰지 마라.
3. 지배 패턴 3~6개를 고르고, 각 패턴을 `edit`, `preserve`, `ask`, `false_positive`로 판정하라. 앵커가 없어도 문단 구조 패턴은 실제 위치 2~3곳을 제시할 수 있다. 근거 없이 횟수를 만들지 마라.
4. 무엇을 고칠지와 무엇을 건드리면 손해인지 같은 비중으로 기록하라.
5. 표준 인간화와 목소리 복원에서는 편집 전에 문단별 `의미 명세`를 잠깐 만들라. 주장·근거·예시·조건·예외·부정·양태·인과 관계와 정도 수식·권고 강도를 원문의 원자 단위로 적고, 윤문본의 각 명제가 그중 하나에 대응하게 하라. `때문에`, `반면`, `다만`, `유연하게`, `~할 필요가 있다`처럼 논리 관계나 판단 강도를 담은 표지는 포장과 구분해 보존하라. 의미 명세는 어구를 그대로 복사하는 틀이 아니라, 새 명제·강화·누락만 막는 대조표다.

## 4. 세 번의 편집을 수행하라

표준 인간화와 목소리 복원에서는 [references/rewriting-playbook.md](references/rewriting-playbook.md)를 읽고 다음 순서를 지켜라.

1. **빼기:** 결산 라벨·의의 과장·기능 없는 연결어·중복 완곡·불필요한 장식을 덜어라.
2. **다시 짜기:** 추상 명사·만능 동사를 가까운 주체와 동사로 돌리고, 기계적 병렬·대구·문단 공식을 실제 논리 진행으로 바꿔라.
3. **목소리 복원:** 같은 원고의 자연스러운 구간에서 문장 시작, 종결, 호흡, 구체성과 직접성의 범위를 찾아 윤문 구간을 그 범위에 맞춰라. 원문에 없는 사실·비유·감정을 만들지 마라.

문단의 주장을 바꾸지 않는 범위에서 문장을 합치거나 나눌 수 있다. 그러나 원문의 제한된 대조를 일반 명제로 강화하거나, 원문에 없던 시간·인과·반복 과정을 넣지 마라. 주장·근거·반론의 순서, 인용 귀속, 행위자, 부정, 양태가 달라질 수 있으면 `ask`로 남겨라. 특정 작가의 고유 표현을 흉내 내지 마라.

의미를 보존한다고 `중요한 시사점`, `핵심적인 습관`, `효율성을 제고한다`같은 원문의 포장까지 남기지 마라. 그 포장 안의 판단 강도만 유지하면서 직접적인 한국어로 다시 쓰라.

윤문이 끝나면 인접한 두 문장의 같은 어근 반복, 불분명한 주체, 원문에 없던 `그저·단지·실은`같은 평가적 부사를 따로 찾아라. 의도한 반복이 아니면 어근을 줄이고, 주체를 복원하고, 새 평가 부사를 지우라. 전역 용어 이관을 했다면 확정어를 다른 말로 흔들지 말고 그 구성어가 정의·제목·표·주변 문장과 겹치는지, 조사·서술어·연어가 맞는지 전 용례를 다시 읽어라.

## 5. 결과를 검증하라

1. `scripts/verify_protected_spans.py`로 보호값을 확인하라.
2. Markdown은 `scripts/verify_markdown_structure.py`로 제목·굵은 표지·이모지·코드 펜스 보존을 확인하라.
3. 수정 대상으로 삼은 결정적 규칙은 `scripts/verify_style_gate.py --target-rule ...`로 실제 감소를 확인하라. 그대로 둔 규칙은 사람이 문맥을 확인한 뒤에만 `--preserve-rule`로 통과시켜라.
4. 목표 표지가 줄지 않은 무수정 결과, 다른 문체 표지를 새로 만든 결과, 보호값이나 Markdown 구조를 잃은 결과는 채택하지 마라.
5. 사실·주체·부정·양태·인과·귀속·누락·격식·목소리를 원문과 대조하라. 윤문본의 명제가 편집 전 `의미 명세`에 없으면 지우거나 `ask`로 남겨라. 강화뿐 아니라 약화도 실패다. `상호작용`을 `반응`으로 줄이거나, `중요하다`를 `도움이 된다`로 낮추거나, `~한다`를 `~할 수 있다`로 바꾸지 마라. 변경률은 경보일 뿐 품질 점수가 아니다.
6. 수정 대상으로 판정한 위치에 `중요한 과정·핵심 수단·효율적으로·실천적 과제·시사점을 제공`같은 공식 포장이 그대로 남거나 유사어로 바뀌었으면 채택하지 마라. 원문의 평가 강도는 직접 서술로 한 번만 남기고 중복 포장을 없애라.
7. 두 후보를 비교할 때에는 [references/comparison-protocol.md](references/comparison-protocol.md)를 따르고, 결정적 gate 뒤에 순서를 가린 사람 평가를 하라.
8. `긴밀히 맞물린다·능동적으로 상호작용한다·삶을 풍요롭게 한다·성장을 위한 핵심 습관` 같은 중요성·성장 결산 묶음이 한 문단이나 결론에 남았는지 다시 보라. 각각이 자연스러워도 한꺼번에 몰리면 KH-S32의 `edit` 후보이며, 짧은 유사어 치환으로 통과시키지 마라.
9. 실제로 실행하지 않은 검증을 receipt에서 `통과`라고 쓰지 마라. 스크립트·원문 대조를 실행했으면 결과를, 실행할 수 없었으면 `미실행`과 사람 확인 필요를 기록하라.
10. 용어를 전역 이관했으면 `scripts/audit_term_migration.py --before ... --after ... --term-map ...`으로 기존어 잔존, 확정어 출현, 구성어 되풀이 후보를 확인하라. 도구가 연어·개념 적합성이나 수렴을 판정한다고 말하지 마라. 각 확정어의 모든 용례를 사람 문맥 판정하지 않았다면 `전수 용어 검토`라고 쓰지 마라.

## 6. 반복 윤문은 수렴시켜라

같은 글을 거듭 검수할 때에는 [references/iteration-contract.md](references/iteration-contract.md)를 읽어라. 첫 윤문 뒤의 입력 내력은 `ai_edited`다. 2차는 최초본 대비 의미·양태·격식 손실과 1차가 새로 만든 결함만 고친다. 3차부터는 기본적으로 진단만 하고, 새로 확인된 결정적 결함이 없으면 직전 채택본을 그대로 반환하라. 수정 횟수를 채우기 위해 표현을 바꾸지 마라.

각 회차는 `baseline`과 `previous`에 모두 대조한다. 특히 주체·부정·정도·시제·가능성·의무·미래 양태를 문장별로 다시 확인하라. 해결한 위치는 **새 의미 손실·새 결정적 표지·사용자 지시가 있을 때만** 다시 열 수 있다. 그 근거 없이 다시 열거나, 줄인 공식을 유사어로 되살리거나, 두 표현 사이를 왕복하면 최근 수정을 버리고 가장 나은 앞 회차로 되돌아가라. 앵커 총계 증감만으로 계속하거나 멈추지 말고 남은 항목의 `edit·preserve·ask·false_positive` 판정을 보라. `scripts/audit_iterations.py`의 `기계 확인 완료`는 수렴 판정이 아니다. 자연스러움과 의미 보존을 사람이 확인하고 `ask·미해결·신규·되살아남`이 없을 때만 수렴이라 하라.

## 7. 결과를 전달하라

윤문 요청에는 설명보다 **완성된 윤문본을 먼저** 제시하라. 뒤에는 다음만 짧게 붙여라.

1. 선택한 모드·입력 내력·검토 범위
2. 해체한 지배 패턴 3~6개
3. 보존한 목소리·인용·논리 장치
4. 확인이 필요한 변경과 검증 결과

반복 검수라면 회차, 최초 기준본, 새 결함·되살아난 결함, 정지 또는 롤백 이유를 한 줄 더 붙여라.

장문 범위 주장과 review ledger는 [references/runtime-contract.md](references/runtime-contract.md)를 따르라. 책 전체의 목차·상호참조·전역 숫자와 미확정 용어 불일치는 manuscript-lint로 넘겨라. 확정한 용어를 이관한 뒤 생긴 동어반복·연어·문장 기능 문제는 K-humanizer가 책임지고 다시 검토하라.

## 조사와 경계

KCI 조사는 [references/kci-query-manifest.json](references/kci-query-manifest.json)과 [references/evidence-status.md](references/evidence-status.md)를 따르라. 자격 증명·원시 응답·비공개 원고·개인 문체 규칙은 공개 저장소에 넣지 마라.

글을 쓴 사람이 사람인지 AI인지 판별하지 말고 AI 탐지 회피를 약속하지 마라. 자연스러운 한국어와 저자 목소리 회복을 평가하라. 입력에 없는 사실·인용·감정을 보태거나 특정 작가를 모사하지 마라.
